## Golang notes.

Golang is a statically typed language that is focused on concurrency and performance (similar to c++)

### Variables.

The := syntax is shorthand for declaring and initializing a variable, e.g. for var f string = "apple" in this case. This syntax is only available inside functions.

Go will infer the type of a variable.
